# Dependencies for 01_data_exploration.ipynb.
#
# Notebooks 02 and 03 live in model/ and use model/requirements.txt.
# Versions match the other components so a single virtual environment
# can serve the whole project without reinstalling on every switch.

pandas==2.3.3
numpy==2.4.4
matplotlib==3.10.8
seaborn==0.13.2
openpyxl==3.1.5          # pandas needs it to read .xlsx

jupyterlab==4.3.0
ipykernel==6.29.5